defaults:
  - base_wall_agent

# Agent configuration
agents:
  designer_agent:
    name: "designer_agent"
    max_turns: 60
    prompt: DESIGNER_AGENT

  planner_agent:
    name: "planner_agent"
    max_turns: 18
    prompt: STATEFUL_PLANNER_AGENT

  critic_agent:
    name: "critic_agent"
    max_turns: 8
    prompt: STATEFUL_CRITIC_AGENT
